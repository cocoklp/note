
bps
	每秒传送的bite数

bytes_sent
	nginx返回给客户端的字节数（8比特），包括响应头和响应体

body_bytes_sent
	nginx返回给客户端的响应体的字节数，不包含响应头

content_length
	nginx从客户端收到的请求头中content-length字段的值

request_length
	请求的字节数，包括请求行，请求头和请求体。

TLD
	top level domain，没有 '.' 就是顶级域名

public suffix：
	可以在其下直接注册域名，又叫eTLD(effective TLD)

eTLD+1:

amazon.com.au  google.com.au —— eTLD+1
maps.google.com  www.google.com —— 不是eTLD+1，share the same eTLD+1
共享eTLD+1的，可以共享cookie



com是顶级域名也是public suffix
au是顶级域名但不是public suffix，注册域名是 amazon.com.au，而不是amazon.au

